import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // App theme base (auth + shell)
  static const Color appBackground = Color(0xFF0A0A0A);
  static const Color appSurface = Color(0xFF141414);
  static const Color appBorder = Color(0xFF2A2A2A);
  static const Color appInputFill = Color(0xFF1A1A1A);
  static const Color appPrimary = Color(0xFFE8E8E8);
  static const Color appOnPrimary = Color(0xFF0A0A0A);
  static const Color appFocusBorder = Color(0xFF888888);

  // Backgrounds
  static const Color background = Color(0xFF313338);
  static const Color surface = Color(0xFF2B2D31);
  static const Color channelBg = Color(0xFF1E1F22);
  static const Color inputBg = Color(0xFF383A40);
  static const Color modalBg = Color(0xFF232428);

  // Brand (still referenced in home/chat/onboarding pages)
  static const Color blurple = Color(0xFF5865F2);
  static const Color blurpleDark = Color(0xFF4752C4);

  // Text
  static const Color textPrimary = Color(0xFFF2F3F5);
  static const Color textSecondary = Color(0xFFB5BAC1);
  static const Color textMuted = Color(0xFF80848E);
  static const Color textLink = Color(0xFF00AFF4);

  // Status
  static const Color online = Color(0xFF23A55A);

  // Error
  static const Color error = Color(0xFFF23F43);

  // Divider
  static const Color divider = Color(0xFF3F4147);
}
